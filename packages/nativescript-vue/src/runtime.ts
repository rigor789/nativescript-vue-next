// This only exports the runtime
// still undecided if we need a runtime-only build for NativeScript
// since the size difference is not an issue
// plus - we might always want to have the compiler included
// for compiling special components on the go?

export * from '@nativescript-vue/runtime'
